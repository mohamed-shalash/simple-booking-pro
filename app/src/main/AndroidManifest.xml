<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trainbooking">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".person_act"></activity>
        <activity android:name=".person_card_view" />
        <activity
            android:name=".person_main_act"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".seat_editer" />
        <activity android:name=".seat_card_view" />
        <activity
            android:name=".seat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".train_edit" />
        <activity android:name=".card_view_train_act" />
        <activity
            android:name=".train_main_act"
            android:label="@string/title_activity_train_main_act"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>